/*Qui definisci tutte le rotte HTTP (tipo GET /users o POST /login). Ogni rotta chiama un controller specifico.

Esempio: userRoutes.js → contiene le rotte relative agli utenti, e collega ogni rotta alla sua funzione nel controller.*/